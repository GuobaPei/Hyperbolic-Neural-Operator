name: Publish project page

on:
  push:
    branches: [main, master]
    paths:
      - "docs/**"
      - "paper-source/**"
      - "scripts/prepare_paper.py"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: gh-pages
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Assemble author-provided paper
        run: cat paper-source/paper.part* > docs/assets/paper.pdf

      - name: Publish docs to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
          publish_branch: gh-pages
          force_orphan: true
